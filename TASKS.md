1) Put hyperparameter tuning into its own file
2) Put model training into its own file
3) Checkpoint the output of hyperparameter tuning so it only needs to be "trained" once
4) Adjust sh files as needed